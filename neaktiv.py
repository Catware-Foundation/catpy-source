"""
messagecust(f"@all ВНИМАНИЕ ВСЕМ ЕПТА!!! зига собирается выгнать нахуй всех неактивов!!!", peer_id)

#hohlinka = randd.choice(hohlinki)
hohlinka = getid("theagrik")
messagecust("неактивы пойманы за яица!!!!!!!!!! ", peer_id)

for ayea in range(10):
    messagecust(f"ебучяя кетвейро-котопаевская рулетка произойдет через {10 - ayea}", peer_id)
    time.sleep(1)

vk.messages.removeChatUser(member_id=hohlinka, random_id=rid(), chat_id=event.chat_id)

message("зига пошел нахуй")

time.sleep(5)
message("сук лан я пошютил")

for ayea in range(10):
    messagecust(f"ебучяя русская рулетка произойдет через {10 - ayea}", peer_id)
    time.sleep(1)

vk.messages.removeChatUser(member_id=getid("nikit0s4"), random_id=rid(), chat_id=event.chat_id)
"""
peer_id = 2000000072
message("ПАПИЩЕКИ ZXC КИК ВТОРОЙ ПАЧКИ НЕАКТИВОВ", disable_mentions=0, reply=False)

for ayea in range(20):
    messagecust(f"ебучяя дремучяя кетвейро-котопаевская рулетка произойдет через {10 - ayea}", peer_id)
    time.sleep(1)

message("сусин когда альбом")
