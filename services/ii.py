
# RIPPED потому что перенесено на отдельный сервер
# Catware Artifical Intelligence - Base
#
# I Ha Bot хуёт Answer база ответов implementation
#
# Самое пиздатое приспособление
#
#
#database_files = os.listdir("databases")
#
#answers = {"фывфыв": ["у тебя говно мотив"]}
#
#for load in database_files:
#    try:
#        cont = ReadFF("databases/" + load)
#        for line in cont.split("\n"):
#            try:
#                answers[str(line.split(ReadFF("sp"))[0]).lower()].append(line.split(ReadFF("sp"))[1])
#            except:
#                answers[str(line.split(ReadFF("sp"))[0]).lower()] = [line.split(ReadFF("sp"))[1]]
#    except:
#        pass

#def getans(txt):
#    typicals = []
#    global answers
#    for ans in list(answers.keys()):
#        for an in ans:
#            if Similar(an, txt):
#                typicals.append(answers[ans][an])
#    return randd.choice(typicals)
